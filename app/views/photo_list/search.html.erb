  <h2>検索結果 "<%= @keyword %>"</h2>
<div class="image">
  <ul>
    <li class="photo_li">
      <% @locations.each do |location| %>
      <%= link_to image_tag(location.photo.url, width: "800px", height: "600px"), "/photo_details/#{location.id}" %>
      <% end %>
    </li>
  </ul>
</div>

