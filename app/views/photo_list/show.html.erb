<h1>投稿一覧リスト</h1>

<% @locations.each do |location| %>
<div class="image">
  <p>
    <%= location.name %><br>
    <% #= image_tag location.photo.url, width: "80%", height: "80%" %>
    <% #= link_to image_tag(location.photo.url width: "100", height: "50%"), "/photo_details/#{location.id}" %>
    <%= link_to image_tag(location.photo.url), "/photo_details/#{location.id}" %>
  </p>
</div>
<% end %>
<hr>
